import React from "react";
import { View, Text } from "react-native";

export default function Device() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Device Settings Screen (next)</Text>
    </View>
  );
}
