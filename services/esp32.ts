import { Mode } from "@/types/dashboard";

// --- Wi-Fi functions (stub for now, we’ll wire later) ---
// Later you’ll replace these with fetch() calls to your ESP32 endpoints.

export async function sendModeToEsp32(_mode: Mode) {
  return;
}

export async function sendFanToEsp32(_fanOn: boolean) {
  return;
}
